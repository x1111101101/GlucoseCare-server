package io.github.x1111101101.glucoseserver

object R {

    val strings = Strings

}

object Strings {
    val invalidHash = "invalid sha256 hash"
    val invalidLoginId = "아이디는 영문 소문자와 숫자로만 이루어져야 합니다."
    val loginIdDuplicated = "다른 회원과 아이디가 중복됩니다."
}